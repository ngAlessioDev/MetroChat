import { PartenzatreniPipe } from './partenzatreni.pipe';

describe('PartenzatreniPipe', () => {
  it('create an instance', () => {
    const pipe = new PartenzatreniPipe();
    expect(pipe).toBeTruthy();
  });
});
